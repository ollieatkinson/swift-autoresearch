# Mutable candidate for native Swift BPE tokenizer training.
# A lower compression ratio means the tokenizer represents the same bytes with fewer tokens.

VOCAB_SIZE=320
MIN_PAIR_FREQUENCY=2
MAX_TRAINING_BYTES=20000
